export enum UserType {
  person = 'person',
  business = 'business',
}

export enum PostType {
  work = 'work',
  business = 'business',
  //수정 필요
}

export enum ReportType {
  business = 'business',
  //수정 필요
}

export enum PayType {
  hourly = 'hourly',
  monthly = 'monthly',
}
